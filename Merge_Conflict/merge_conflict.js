/*
    *** Merge Conflict ***
        => When we are going to work main/master and another branch, 
        and at a same time, we are going to commit same branch same line, 
        in that time we are facing merge confict, both branch when we want to merge with the main/master.

    => When we face this problem, we should resolve this confict by deleteing same code, which we do not want. 
*/
console.log(10);